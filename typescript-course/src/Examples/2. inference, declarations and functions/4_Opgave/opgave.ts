// 1. Make a function that takes a array of strings, and gives back the last element of the array. If the array is empty, return null.

// 2. Use this type for the function parameter and return type.

// 3. Use type assertion to make sure that the array is not null before returning the last element.

// 4. Use the function in a console.log statement.

import { stringArray } from "./types/type";

function opgave() {}  // Continue here

